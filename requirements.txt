# GELLO leader-arm software — runtime dependencies.
#
# Core: the Feetech servo GUI, scanner, and calibration tools.
numpy==1.26.4
pyserial==3.5
websockets==13.1

# Real-robot teleoperation (software/lerobot/so100/*.py).
# Install LeRobot with the [feetech] extra from source — it is not pinned here:
#   https://github.com/huggingface/lerobot
# lerobot
